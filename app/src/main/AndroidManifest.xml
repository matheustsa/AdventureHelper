<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mtsa.adventurehelper">

    <!--<uses-permission android:name="android.permission.WRITE_SETTINGS"/>-->

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <!--<activity android:name=".TelaPrincipal">-->
        <activity android:name=".Principal.TelaPrincipal">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Principal.TelaDados" />
        <activity android:name=".Principal.TelaListaPersonagens" />
        <activity android:name=".Principal.TelaPersonagem" />
        <activity android:name=".Principal.EditarFicha" />
        <activity android:name=".Principal.FichaCompleta" />
        <activity android:name=".Principal.CriarPersonagem" />
    </application>

</manifest>